apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: {{ .Values.sample-application_metadata_name }}
  namespace: {{ .Values.sample-application_metadata_namespace }}
spec:
  destination:
    name: {{ .Values.sample-application_destination_name }}
    namespace: {{ .Values.sample-application_destination_namespace }}
  project: {{ .Values.sample-application_project }}
  source:
    helm:
      valueFiles:
        - values.yaml
    path: {{ .Values.sample-application_source_path }} 
    repoURL: {{ .Values.sample-sample-application_source_repo_url }}
    targetRevision: HEAD
  syncPolicy:
    retry:
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m0s
      limit: 2